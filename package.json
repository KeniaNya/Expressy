{
  "name": "expressy-workspace",
  "private": true,
  "workspaces": [
    "packages/*",
    "demo"
  ],
  "scripts": {
    "test": "bun test",
    "demo": "bun run --cwd demo dev",
    "publish:expressy": "npm publish --workspace expressy-bun"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "nunjucks": "^3.2.4"
  },
  "version": "1.0.1"
}
